import { useCallback, useEffect, useRef, useState } from 'react'
import { ArrowRight, ArrowUpRight, BookOpen, Building2, Check, ChevronDown, ChevronRight, CircleHelp, FileStack, FileText, FolderKanban, GitCompareArrows, History, LayoutDashboard, Menu, Network, Play, Plus, Search, Settings2, ShieldAlert, Sparkles, X } from 'lucide-react'
import { api, date } from './api'
import { WorkspaceContext } from './context'
import type { Health, Project, ProjectList, Result, Source, View } from './types'
import { Badge, Empty, Modal, Spinner, labels, statusLabels } from './components/UI'
import SourceViewer from './components/SourceViewer'
import Overview from './pages/Overview'
import Documents from './pages/Documents'
import Comparison from './pages/Comparison'
import Structure from './pages/Structure'
import Findings from './pages/Findings'
import Report from './pages/Report'
import Settings from './pages/Settings'

const nav = [{id:'overview',icon:LayoutDashboard},{id:'documents',icon:FileStack},{id:'comparison',icon:GitCompareArrows},{id:'structure',icon:Network},{id:'findings',icon:ShieldAlert},{id:'report',icon:FileText}] as const
const pages = {overview:Overview,documents:Documents,comparison:Comparison,structure:Structure,findings:Findings,report:Report,settings:Settings}
function readView(): View { const hash=window.location.hash.slice(1);return hash in labels?hash as View:'overview' }

export default function App(){
  const [view,setView]=useState<View>(readView),[projects,setProjects]=useState<ProjectList[]>([]),[project,setProject]=useState<Project|null>(null)
  const [projectId,setProjectId]=useState(localStorage.getItem('atlas-project')||''),[health,setHealth]=useState<Health|null>(null)
  const [error,setError]=useState(''),[loading,setLoading]=useState(true),[search,setSearch]=useState(''),[mobileOpen,setMobileOpen]=useState(false)
  const [modal,setModal]=useState<'new'|'projects'|'history'|'help'|'analyze'|null>(null),[source,setSource]=useState<{id:string;highlight?:string}|null>(null)
  const [toasts,setToasts]=useState<{id:number;text:string;error:boolean}[]>([]),[busy,setBusy]=useState(false),[mode,setMode]=useState('auto')
  const [historyResult,setHistoryResult]=useState<Result|null>(null)
  const activeId=useRef(projectId);activeId.current=projectId
  const notify=useCallback((text:string,error=false)=>{const id=Date.now()+Math.random();setToasts(items=>[...items,{id,text,error}]);window.setTimeout(()=>setToasts(items=>items.filter(t=>t.id!==id)),error?12000:5500)},[])
  const navigate=useCallback((next:View)=>{window.location.hash=next;setView(next);setMobileOpen(false);window.scrollTo({top:0,behavior:'instant'})},[])
  const reload=useCallback(async()=>{if(!projectId)return;const [detail,list]=await Promise.all([api<Project>(`/projects/${projectId}`),api<ProjectList[]>('/projects')]);if(activeId.current===projectId){setProject(detail);setProjects(list);setError('')}},[projectId])
  async function initialize(){
    setLoading(true);setError('')
    try{const [list,status]=await Promise.all([api<ProjectList[]>('/projects'),api<Health>('/health')]);setProjects(list);setHealth(status);if(!list.some(p=>p.id===activeId.current))setProjectId(list[0]?.id||'')}
    catch(e){setError((e as Error).message)}finally{setLoading(false)}
  }
  useEffect(()=>{void initialize();const onHash=()=>setView(readView());window.addEventListener('hashchange',onHash);return()=>window.removeEventListener('hashchange',onHash)},[])
  useEffect(()=>{if(projectId){localStorage.setItem('atlas-project',projectId);setProject(null);setSource(null);void reload().catch(e=>setError(e.message))}},[projectId,reload])
  useEffect(()=>{if(project?.status!=='running')return;const interval=window.setInterval(()=>{void reload().catch(e=>setError(e.message))},1500);return()=>window.clearInterval(interval)},[project?.status,reload])
  const openSource=useCallback((s:Source)=>setSource({id:s.document_id,highlight:s.id.split(':').pop()}),[])
  async function create(name:string,organization:string){setBusy(true);try{const p=await api<Project>('/projects',{method:'POST',body:JSON.stringify({name,organization})});setProjects(await api<ProjectList[]>('/projects'));setProjectId(p.id);setModal(null);navigate('documents');notify('Проект создан. Добавьте исходные документы.')}catch(e){notify((e as Error).message,true)}finally{setBusy(false)}}
  async function createDemo(){setBusy(true);try{const p=await api<Project>('/projects/demo',{method:'POST'});setProjects(await api<ProjectList[]>('/projects'));setProjectId(p.id);navigate('overview');setModal(null);notify('Демонстрационный проект создан и проанализирован')}catch(e){notify((e as Error).message,true)}finally{setBusy(false)}}
  async function startAnalysis(){if(!project)return;setBusy(true);try{await api(`/projects/${project.id}/analyze`,{method:'POST',body:JSON.stringify({mode})});setModal(null);await reload();notify('Анализ запущен. Можно оставаться на любой странице проекта.')}catch(e){notify((e as Error).message,true)}finally{setBusy(false)}}
  async function showHistory(id:string){try{setHistoryResult(await api<Result>(`/projects/${projectId}/runs/${id}`))}catch(e){notify((e as Error).message,true)}}
  const Page=pages[view]
  const pending=project?.result?.findings.filter(f=>f.status==='pending').length||0
  const completed=project?.status==='completed'
  return <div className="app-shell"><a className="skip-link" href="#main-content" onClick={e=>{e.preventDefault();document.getElementById('main-content')?.focus()}}>Перейти к содержимому</a>{mobileOpen&&<button className="sidebar-overlay" onClick={()=>setMobileOpen(false)} aria-label="Закрыть меню"/>}<aside className={`sidebar ${mobileOpen?'open':''}`}><button className="brand" onClick={()=>navigate('overview')} aria-label="ATLAS — главная"><span className="brand-symbol"><Network size={25}/></span><span>atlas<span className="brand-period">.</span><small>ОРГАНИЗАЦИЯ В ДЕТАЛЯХ</small></span></button><button className="workspace-switch" onClick={()=>setModal('projects')}><span className="workspace-icon"><Building2 size={18}/></span><span><strong>Рабочее пространство</strong><small>{project?.organization.split(' · ')[0]||'HackAlem AI'}</small></span><ChevronDown size={14}/></button><div className="nav-heading">АНАЛИЗ ОРГАНИЗАЦИИ</div><nav aria-label="Главная навигация">{nav.map(({id,icon:Icon})=><button className={`nav-item ${view===id?'active':''}`} key={id} onClick={()=>navigate(id)}><Icon size={19}/><span>{labels[id]}</span>{id==='findings'&&pending>0&&<span className="nav-count">{pending}</span>}{id==='documents'&&project&&<span className="nav-count neutral">{project.documents.length}</span>}</button>)}</nav><div className="sidebar-bottom"><div className="sidebar-note"><span className="note-spark"><Sparkles size={18}/></span><h3>Решения с основанием</h3><p>Каждый вывод — с опорой на ваши документы.</p><button onClick={()=>setModal('help')}>Как работает ATLAS<ArrowUpRight size={14}/></button></div><button className={`nav-item ${view==='settings'?'active':''}`} onClick={()=>navigate('settings')}><Settings2 size={19}/><span>Настройки</span></button><button className="nav-item" onClick={()=>setModal('help')}><CircleHelp size={19}/><span>Помощь и методология</span></button><div className="sidebar-profile"><span className="avatar">BL</span><div><strong>Команда Blitz</strong><span>HackAlem AI 2026</span></div><span className="online-dot"/></div></div></aside><div className="main-shell"><header className="topbar"><div className="breadcrumbs"><button className="icon-button mobile-menu" onClick={()=>setMobileOpen(true)} aria-label="Открыть меню"><Menu size={20}/></button><span>Рабочее пространство</span><ChevronRight size={14}/><b>{labels[view]}</b></div><div className="topbar-right"><button className="top-search" onClick={()=>{navigate('comparison');window.setTimeout(()=>document.querySelector<HTMLInputElement>('input[aria-label="Поиск функций"]')?.focus(),50)}}><Search size={17}/><span>Поиск функций</span></button><button className="icon-button" onClick={()=>setModal('help')} aria-label="Помощь"><CircleHelp size={19}/></button><span className="topbar-divider"/><span className="avatar small">BL</span></div></header><main id="main-content" tabIndex={-1}><div className="project-bar"><div><span className="project-breadcrumb"><FolderKanban size={14}/>ПРОЕКТ</span><button className="project-title" onClick={()=>setModal('projects')}>{project?.name||'Ваши проекты'}<ChevronDown size={15}/></button>{project?.is_demo&&<span className="demo-label">ДЕМО</span>}</div><div className="project-actions"><button className="button secondary icon-mobile" onClick={()=>{setModal('history');setHistoryResult(null)}} disabled={!project?.runs.length}><History size={16}/><span>История</span></button><button className="button primary" disabled={!project||project.status==='running'} onClick={()=>setModal('analyze')}>{project?.status==='running'?<Spinner/>:<Sparkles size={16}/>}<span>{project?.status==='running'?'Анализируем…':completed?'Повторить анализ':'Запустить анализ'}</span></button></div></div>
    {project?.status==='running'&&<div className="analysis-progress" role="status"><div><Spinner/><strong>{project.stage}</strong><span>{project.progress}%</span></div><div className="progress-track"><span style={{width:`${project.progress}%`}}/></div><small>Извлечение и сопоставление могут занять несколько минут. Результат сохранится автоматически.</small></div>}
    {project?.status==='failed'&&<div className="error-box" role="alert"><strong>Анализ не завершён</strong><p>{project.error}</p>{project.result&&<p>Ниже показан предыдущий успешный результат.</p>}</div>}
    {error?<div className="error-box"><h2>Не удалось загрузить рабочее пространство</h2><p>{error}</p><button className="button secondary" onClick={()=>{void initialize();void reload().then(()=>setError('')).catch(()=>{})}}>Повторить</button></div>:loading||(!project&&projectId)?<div className="page-loading"><Spinner/><span>Загружаем рабочее пространство…</span></div>:!project?<Empty title="Создайте первый проект" text="Начните собственный анализ или исследуйте демонстрационный комплект." action={<><button className="button primary" onClick={()=>setModal('new')}><Plus size={17}/>Новый проект</button><button className="button secondary" disabled={busy} onClick={()=>void createDemo()}>Открыть демо</button></>}/>:<WorkspaceContext.Provider value={{project,health,reload,navigate,openSource,openDocument:id=>setSource({id}),notify,search,setSearch}}><div className="page-content" key={`${project.id}-${view}`}><Page/></div>{source&&<SourceViewer id={source.id} highlight={source.highlight} close={()=>setSource(null)}/>}</WorkspaceContext.Provider>}
    <footer className="app-footer"><span><span className="online-dot"/>ATLAS · HackAlem AI</span><span>Понятная структура. Обоснованные решения.</span><button onClick={()=>navigate('settings')}><span className={`engine-dot ${health?.gpt_configured?'gpt':''}`}/>{health?.gpt_configured?'GPT подключён':'Локальный режим'}</button></footer></main></div>
    <div className="toast-stack" aria-live="polite">{toasts.map(t=><div key={t.id} className={`toast ${t.error?'error':''}`}>{t.error?<CircleHelp size={18}/>:<Check size={18}/>}<span>{t.text}</span><button className="icon-button" onClick={()=>setToasts(items=>items.filter(i=>i.id!==t.id))} aria-label="Скрыть уведомление"><X size={15}/></button></div>)}</div>
    {modal==='new'&&<Modal title="Новый проект анализа" close={()=>setModal(null)}><NewProjectForm busy={busy} create={create}/></Modal>}
    {modal==='projects'&&<Modal title="Проекты" close={()=>setModal(null)}><div className="modal-body"><p className="muted">Каждый проект хранит свой комплект документов и историю анализа.</p><div className="project-list">{projects.map(p=><button className={projectId===p.id?'selected':''} key={p.id} onClick={()=>{setProjectId(p.id);setModal(null);navigate('overview')}}><span className="workspace-icon"><FolderKanban size={20}/></span><span><strong>{p.name}</strong><small>{p.document_count} документов · {statusLabels[p.status]}{p.is_demo?' · Демо':''}</small></span>{projectId===p.id&&<Check size={18}/>}</button>)}</div><div className="modal-actions"><button className="button secondary" onClick={()=>void createDemo()} disabled={busy}>{busy?<Spinner/>:<Play size={15}/>}Демо-проект</button><button className="button primary" onClick={()=>setModal('new')}><Plus size={17}/>Новый проект</button></div></div></Modal>}
    {modal==='analyze'&&project&&<Modal title="Запуск анализа" close={()=>setModal(null)}><div className="modal-body"><div className="analysis-dialog-intro"><Sparkles size={25}/><h3>Сравним функции и сохраним основания выводов</h3></div><p>В проекте {project.documents.filter(d=>d.phase==='before').length} документов «до» и {project.documents.filter(d=>d.phase==='after').length} «после». Результат появится в обзоре, а предыдущий запуск сохранится в истории.</p><label className="form-label">Режим анализа<select value={mode} onChange={e=>setMode(e.target.value)}><option value="auto">{health?.gpt_configured?`GPT · ${health.model} (рекомендуется)`:'Локальный алгоритм (GPT не подключён)'}</option><option value="local">Локальный алгоритм · без API</option><option value="gpt" disabled={!health?.gpt_configured}>GPT · смысловое сопоставление</option></select></label><div className="info-strip">{mode==='gpt'||(mode==='auto'&&health?.gpt_configured)?'Извлечённый текст будет отправлен в OpenAI. Используются API-кредиты проекта.':'Локальный анализ не использует GPT. Он находит сходные формулировки и требует ручной проверки смысловых соответствий.'}</div><div className="modal-actions"><button className="button secondary" onClick={()=>setModal(null)}>Отмена</button><button className="button primary" onClick={()=>void startAnalysis()} disabled={busy||!project.documents.some(d=>d.phase==='before')||!project.documents.some(d=>d.phase==='after')}>{busy?<Spinner/>:<Play size={16}/>}Начать анализ</button></div>{(!project.documents.some(d=>d.phase==='before')||!project.documents.some(d=>d.phase==='after'))&&<p className="form-error">Для запуска нужен хотя бы один документ в каждом комплекте.</p>}</div></Modal>}
    {modal==='history'&&<Modal title="История анализа" close={()=>setModal(null)}><div className="modal-body">{project?.runs.map((run,i)=><button className="history-row" key={run.id} onClick={()=>void showHistory(run.id)}><History size={18}/><span><strong>Анализ от {date(run.created_at)}</strong><small>Редакция документов {run.revision}{i===0?' · последний запуск':''}</small></span><ChevronRight size={16}/></button>)}{historyResult&&<div className="history-detail"><Badge type="completed">{historyResult.engine}</Badge><p>{historyResult.summary}</p><small>{historyResult.findings.filter(f=>f.status!=='pending').length} замечаний проверено · {historyResult.document_count} документов</small></div>}</div></Modal>}
    {modal==='help'&&<Modal title="Как работать с ATLAS" close={()=>setModal(null)}><div className="modal-body help-content"><span className="help-symbol"><BookOpen size={30}/></span><h3>От двух комплектов — к ясному заключению</h3>{[{n:'1',title:'Добавьте документы',text:'В разделе «Документы» загрузите положения и инструкции до и после реорганизации. Проверьте распознанные функции.'},{n:'2',title:'Запустите анализ',text:'GPT сопоставит обязанности по смыслу. Без ключа доступен локальный режим для контрольных примеров.'},{n:'3',title:'Проверьте источники',text:'Откройте замечание и нажмите на ссылку документа. Нужный фрагмент будет выделен в боковой панели.'},{n:'4',title:'Зафиксируйте решение',text:'Подтвердите или отклоните замечания, добавьте комментарии и экспортируйте заключение.'}].map(s=><div className="help-step" key={s.n}><span>{s.n}</span><div><h4>{s.title}</h4><p>{s.text}</p></div></div>)}<button className="button primary" onClick={()=>{setModal(null);navigate('documents')}}>К документам<ArrowRight size={16}/></button></div></Modal>}
  </div>
}
function NewProjectForm({busy,create}:{busy:boolean;create:(name:string,organization:string)=>void}){const [name,setName]=useState(''),[organization,setOrganization]=useState('');return <form className="modal-body" onSubmit={e=>{e.preventDefault();create(name,organization)}}><p className="muted">Отдельное рабочее пространство для одного сценария реорганизации.</p><label className="form-label">Название проекта<input autoFocus required maxLength={120} placeholder="Например, реорганизация IT-департамента" value={name} onChange={e=>setName(e.target.value)}/></label><label className="form-label">Организация <span className="muted">· необязательно</span><input maxLength={120} placeholder="Название вашей организации" value={organization} onChange={e=>setOrganization(e.target.value)}/></label><div className="modal-actions"><button className="button primary" disabled={busy||!name.trim()}>{busy?<Spinner/>:<Plus size={17}/>}Создать проект</button></div></form>}
